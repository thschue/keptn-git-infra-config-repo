namePrefix: "dev-"
resources:
  - gke.yaml
  - helm.yaml
  - kubernetes.yaml
  - provider.yaml

patchesStrategicMerge:
  - provider-patch.yaml
  - cluster-patch.yaml
  - helm-patch.yaml
  - kubectl-patch.yaml